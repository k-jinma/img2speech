# 画像文字認識アプリのインストールガイド

このガイドは、画像から文字を読み取って音声で読み上げるアプリのインストール方法を説明します。

## はじめに

このアプリを使うと、写真やスクリーンショットに写っている文字をコンピューターに読み取らせて、その内容を声で読んでもらうことができます。たとえば、黒板の写真を撮って文字を読み取ったり、外国語の看板を写真に撮って日本語に翻訳する前の文字データを取り出したりできます。

今回は「仮想環境」という特別な箱の中にアプリを入れます。仮想環境とは、このプロジェクト専用の独立した作業スペースのことです。これを使うと、あなたの Mac にすでに入っているほかのプログラムやアプリに影響を与えずに、安全に新しいプログラムを試すことができます。

## このアプリでできること

このアプリには2つの主な機能があります。

1つ目は **OCR（文字認識）** です。OCR は Optical Character Recognition の略で、画像の中の文字を読み取ってテキストに変換する技術です。たとえば、教科書のページを写真に撮ったら、その写真から文字だけを取り出すことができます。

2つ目は **音声読み上げ（TTS）** です。TTS は Text To Speech の略で、文字をコンピューターの声で読み上げる機能です。OCR で読み取った文字を、Mac の音声合成機能を使って声に出して読んでくれます。

## 作業の流れ

今日やることを順番に説明します。

まず、仮想環境という専用の作業スペースを作ります。次に、アプリが動くために必要な「ライブラリ」という道具セットをインストールします。ライブラリとは、プログラムを作るときに便利な機能をまとめたもので、画像を処理する道具や文字を認識する道具などがセットになっています。

その後、テストプログラムを実行して、すべてが正しくインストールされているか確認します。最後に、実際にアプリを動かして、画像から文字を読み取って音声で読み上げる体験をします。

---

## ステップ1：プロジェクトを VS Code で開く

まず、VS Code でこのプロジェクトを開きます。

VS Code を起動したら、画面上部のメニューから「ファイル」をクリックし、「フォルダを開く…」を選びます。ファイル選択の画面が出たら、img2speech フォルダを探して選択し、「開く」ボタンをクリックしてください。

すると、VS Code の左側にプロジェクト内のファイル一覧が表示されます。requirements.txt や ocr_from_path.py といったファイルが見えていれば、正しくフォルダを開けています。

---

## ステップ2：ターミナルを開く

次に、コマンドを入力するための「ターミナル」を開きます。

ターミナルとは、文字で命令を入力する場所のことです。マウスでボタンをクリックする代わりに、文字で「これをやって」と指示を出すことができます。プログラムを使う人は、ターミナルをよく使います。

VS Code の画面上部のメニューから「表示」をクリックし、「ターミナル」を選びます。すると、画面の下半分にターミナルが開きます。ここに文字を入力することで、コンピューターに命令を出すことができます。

ターミナルを開いたら、まず自分がどこの場所（フォルダ）にいるか確認しましょう。ターミナルの文字を見ると、最後の方に「img2speech」という文字が入っていれば、正しい場所にいます。

---

## ステップ3：仮想環境を作る（最初の1回だけ）

ここから、仮想環境という専用の作業スペースを作ります。この作業は、最初の1回だけやれば大丈夫です。

仮想環境とは、このプロジェクト専用の Python の実験箱のようなものです。あなたの Mac には、すでに Python がインストールされているかもしれませんし、ほかのプロジェクトで使っている Python があるかもしれません。仮想環境を使うと、それらに影響を与えずに、このプロジェクトだけのために新しいライブラリをインストールできます。

次のコマンドを入力して Enter キーを押してください。

```bash
python3 -m venv .venv
```

このコマンドは「python3 を使って、venv（仮想環境）というモジュールを動かして、.venv という名前のフォルダに仮想環境を作ってください」という意味です。`.venv` というフォルダ名は、「隠しフォルダ（ドットで始まるフォルダ）で venv（仮想環境）用」という意味でよく使われる名前です。

実行すると、数秒かかることがありますが、エラーが出なければ成功です。

---

## ステップ4：仮想環境を有効にする（実験箱に入る）

仮想環境を作ったら、次はその中に「入る」必要があります。入ることで、これから実行するコマンドやインストールするライブラリが、すべてこの仮想環境の中だけで動くようになります。

次のコマンドを入力してください。

```bash
source .venv/bin/activate
```

このコマンドは「.venv フォルダの中にある activate というスクリプトを実行してください」という意味です。Mac では `source` というコマンドを使います（Windows の場合は違うコマンドになります）。

コマンドを実行すると、ターミナルの一番左側に `(.venv)` という文字が表示されます。これが表示されていれば、仮想環境に正しく入れたという印です。これから先は、すべての作業がこの仮想環境の中で行われます。

---

## ステップ5：インストール用の道具を最新にする

Python のライブラリをインストールするには、`pip`（ピップ）という道具を使います。pip は「パッケージインストーラー」という、ライブラリを簡単にインストールするための便利なプログラムです。

まず、この pip 自体を最新のバージョンにアップデートします。古いバージョンのままだと、新しいライブラリがうまくインストールできないことがあるからです。

次のコマンドを入力してください。

```bash
python -m pip install --upgrade pip setuptools wheel
```

このコマンドは「python の中にある pip モジュールを使って、pip と setuptools と wheel を最新版にアップグレードしてください」という意味です。setuptools と wheel は、ライブラリをインストールするときに裏で使われる補助ツールです。

「Python パッケージをグローバル環境にインストールしている可能性があります。これにより、パッケージのバージョン間で競合が発生する可能性があります。これらのパッケージを使用して仮想環境を作成し、依存関係を分離しますか?」とメッセージが出たら「はい」を選択します。

そして、VSCodeの画面の上のメッセージから「既存のものを使用」を選びます。

実行すると、「Requirement already satisfied」や「Successfully installed」といったメッセージが表示されます。エラーが出なければ成功です。

---

## ステップ6：必要なライブラリをインストールする

いよいよ、このアプリが動くために必要なライブラリをインストールします。

このプロジェクトには `requirements.txt` というファイルがあります。このファイルには、「このアプリを動かすために必要なライブラリの名前とバージョン」がリスト形式で書かれています。pip はこのファイルを読んで、必要なものをすべて自動でインストールしてくれます。

次のコマンドを入力してください。

```bash
pip install -r requirements.txt
```

`-r` は「ファイルを読み込んで、その中に書かれているものをインストールしてください」という意味のオプションです。

このコマンドを実行すると、たくさんのライブラリがダウンロードされ、インストールされます。インストールには数分かかることがあります。ターミナルにはたくさんの文字が流れますが、心配しないでください。最後に「Successfully installed」と表示されて、エラーが出なければ成功です。

### もしエラーが出たら

特に `torch`（トーチ）や `torchvision`（トーチビジョン）というライブラリでエラーが出ることがあります。これは、機械学習（AI）の計算をするための大きなライブラリで、Mac の種類や Python のバージョンによっては、自動で見つけられないことがあります。

もしエラーが出たら、次のコマンドを試してください。これは、CPU 専用版の PyTorch を公式の配布場所から直接ダウンロードするコマンドです。

```bash
pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
```

`--index-url` は「この URL からライブラリを探してダウンロードしてください」という意味です。

---

## ステップ7：OCR のモデルを先にダウンロードする（おすすめ）

文字認識（OCR）をするには、「学習済みモデル」というデータが必要です。学習済みモデルとは、たくさんの文字の画像を使ってあらかじめ学習させた AI のことで、これを使うことで新しい画像の文字も正しく認識できるようになります。

このモデルは、アプリを最初に動かすときに自動でダウンロードされますが、先にダウンロードしておくとテストがスムーズに進みます。モデルのファイルサイズは大きいので、ダウンロードには数分かかります。

次のコマンドを入力してください。

```bash
python - <<'PY'
import easyocr, os
os.makedirs('./models', exist_ok=True)
_ = easyocr.Reader(['ja'], gpu=False, model_storage_directory='./models', download_enabled=True)
print('モデルのダウンロードが完了しました。')
PY
```

このコマンドは少し複雑ですが、簡単に言うと「Python のプログラムを実行して、日本語の文字認識モデルを ./models というフォルダにダウンロードしてください」という意味です。

`gpu=False` は「GPU（グラフィック処理専用のチップ）を使わず、CPU で動かしてください」という設定です。GPU がない Mac でも動くようにしています。

実行中は「Downloading...」といったメッセージが表示され、しばらく待つと「モデルのダウンロードが完了しました。」と表示されます。

---

## ステップ8：テストを実行して確認する

ここまでの作業がうまくいったか、テストプログラムで確認しましょう。

このプロジェクトには `test_app.py` というテスト用のプログラムが入っています。このプログラムを実行すると、必要なライブラリが正しくインストールされているか、OCR や音声読み上げが動くかを自動でチェックしてくれます。

次のコマンドを入力してください。

```bash
python test_app.py
```

実行すると、次のような結果が表示されます。

```
画像文字認識・音声読み上げアプリ テスト開始
==================================================
ライブラリインポートテスト:
✓ EasyOCR インポート成功
✓ pyttsx3 インポート成功
✓ OpenCV インポート成功
✓ PIL/Pillow インポート成功
✓ tkinter インポート成功

EasyOCR機能テスト:
✓ EasyOCR初期化成功（ローカルモデル使用）

pyttsx3機能テスト:
✓ pyttsx3初期化成功
✓ 音声プロパティ設定成功

GUI機能テスト:
✓ GUIウィジェット作成成功

==================================================
🎉 すべてのテストが成功しました！
```

すべて「✓（チェックマーク）成功」と表示されればOKです。もし「✗ 失敗」が出たら、その前の手順のどこかでうまくいっていない可能性があります。

もし EasyOCR のテストだけが失敗している場合は、ステップ7（モデルのダウンロード）をもう一度実行してから、再度テストしてください。

---

## ステップ9：アプリを実際に動かしてみる

テストが成功したら、実際にアプリを動かしてみましょう。このプロジェクトには2つのプログラムがあります。

### コマンドライン版（シンプル版）

1つ目は `ocr_from_path.py` というシンプルなプログラムです。これは、画像ファイルから文字を読み取って、ターミナルに表示するプログラムです。

次のコマンドで実行できます。

```bash
python ocr_from_path.py
```

実行すると、プログラムが画像を読み込んで、認識した文字をターミナルに表示します。どの画像を読み込むかは、プログラムの中で設定されています（`IMAGE_PATH` という変数で指定されています）。

### 画面付きアプリ（GUI 版）

2つ目は `image_to_speech_app.py` という、画面がついた本格的なアプリです。こちらは、画像を選んで、文字を認識して、さらに音声で読み上げることができます。

次のコマンドで実行できます。画面下段のDockにアプリケーションが起動します。

```bash
python image_to_speech_app.py
```

実行すると、ウィンドウが開きます。使い方は次の通りです。

1. 「画像を選択」ボタンをクリックして、文字が写っている画像ファイル（写真やスクリーンショットなど）を選びます
2. 「文字認識実行」ボタンをクリックすると、画像から文字が読み取られ、右側のテキストエリアに表示されます
3. 「音声読み上げ」ボタンをクリックすると、認識した文字を Mac の音声合成機能で読み上げます

音が出ない場合は、Mac の音量を確認してください。

---

## 次回からの使い方

一度インストールが終わったら、次回からは簡単です。

プロジェクトを使うたびに、VS Code でプロジェクトを開いたら、いつものように▶️ボタンを押して実行してください。

---

## 困ったときの対処法

作業中にエラーが出ることがあります。よくあるエラーと、その対処法を説明します。

### pip が見つからないと言われる

「command not found: pip」や「pip: command not found」といったエラーが出ることがあります。これは、pip が見つからないという意味です。

この場合は、`pip` の代わりに `python -m pip` を使ってください。`-m` は「モジュールとして実行する」という意味で、Python を通して pip を動かすことができます。

```bash
python -m pip install -r requirements.txt
```

### torch や torchvision でエラーが出る

「ERROR: Could not find a version that satisfies the requirement torch」といったエラーが出ることがあります。これは、あなたの Mac や Python のバージョンに合った PyTorch が見つからないという意味です。

PyTorch（torch/torchvision）は機械学習用の大きなライブラリで、Mac の種類（Intel チップか Apple Silicon チップか）や Python のバージョンによって、使えるバージョンが違います。

このエラーが出たら、CPU 専用版を公式の配布場所から直接ダウンロードしてください。

```bash
pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
```

### 文字が認識できない、または声が出ない

アプリを実行しても文字が認識されなかったり、音声が出なかったりすることがあります。

まず、もう一度同じコマンドを実行してみてください。1回目は失敗しても、2回目は成功することがあります。

音声が出ない場合は、Mac の音量設定を確認してください。音量が0になっていたり、ミュートになっていたりすることがあります。

それでも直らない場合は、ターミナルに表示されたエラーメッセージをメモして、先生や担当者に見せてください。エラーメッセージには、何が問題なのかのヒントが書かれています。

### GUI（画面付きアプリ）が起動しない

`image_to_speech_app.py` を実行してもウィンドウが開かない場合、tkinter というライブラリに問題がある可能性があります。

tkinter は、Python で画面付きアプリを作るための標準ライブラリですが、Mac によっては正しくインストールされていないことがあります。

この問題は少し複雑なので、ターミナルに表示されたエラーメッセージをそのまま先生や担当者に見せてください。場合によっては、Python 本体を再インストールする必要があるかもしれません。

### ネットワークのエラー（SSL やタイムアウト）

「SSL: CERTIFICATE_VERIFY_FAILED」や「Timeout」といったエラーが出ることがあります。これは、インターネット接続に問題があるという意味です。

まず、Mac が Wi-Fi に接続されているか確認してください。学校や会社のネットワークでは、セキュリティの設定で一部のダウンロードがブロックされていることがあります。

可能であれば、自宅の Wi-Fi など、別のネットワークで試してみてください。

---

## 用語解説

このガイドで使われている専門用語を、わかりやすく説明します。

**仮想環境（Virtual Environment）**
プロジェクト専用の独立した Python 環境のことです。図書館で例えると、みんなが使う共有の本棚ではなく、自分専用のロッカーのようなものです。ここにインストールしたライブラリは、ほかのプロジェクトに影響しません。

**ライブラリ（Library）**
便利な機能をまとめたプログラムの部品集です。たとえば、画像を処理する機能（OpenCV）、機械学習の計算をする機能（PyTorch）、文字を認識する機能（EasyOCR）などがあります。自分で一から作ると大変なことも、ライブラリを使えば簡単にできます。

**OCR（Optical Character Recognition）**
光学式文字認識のことで、画像の中の文字を読み取ってテキストに変換する技術です。スキャナーで書類を読み取るときに使われる技術と同じです。

**TTS（Text To Speech）**
テキスト読み上げのことで、文字をコンピューターの声で読む技術です。Mac には最初から音声合成機能が入っているので、それを使って文字を声に変換します。

**ターミナル（Terminal）**
文字で命令を入力する場所のことです。コマンドライン、コンソール、シェルなどとも呼ばれます。マウスでボタンをクリックする代わりに、文字で「○○をやって」と指示を出すことができます。

**VS Code（Visual Studio Code）**
マイクロソフトが作っている、プログラムを書くための無料ソフトです。エディターとも呼ばれます。メモ帳の高機能版のようなもので、プログラムを書きやすくする便利な機能がたくさん入っています。

**pip（Package Installer for Python）**
Python のライブラリを簡単にインストールするための道具です。「ピップ」と読みます。インターネットから必要なライブラリを探してきて、自動でインストールしてくれます。

**Python**
プログラミング言語の一つです。読みやすく書きやすいので、初心者にも人気があります。機械学習やデータ分析、ウェブアプリなど、いろいろなことに使われています。

**コマンド（Command）**
コンピューターへの命令のことです。ターミナルに文字で入力します。たとえば「cd」は移動の命令、「python」は Python を実行する命令です。

---

## さいごに

お疲れさまでした！ここまでの手順で、画像文字認識アプリを自分の Mac で動かすことができるようになりました。

もし途中でわからないことがあったり、エラーが出て進めなくなったりしたら、一人で悩まずに、先生や周りの人に聞いてください。プログラミングは、最初は誰でもうまくいかないことがあります。大切なのは、あきらめずに一つずつ問題を解決していくことです。

このアプリを使って、いろいろな画像の文字を読み取ってみてください。楽しく学習できることを願っています！
